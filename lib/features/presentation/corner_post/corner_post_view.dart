import 'package:flutter/material.dart';

class CornerPostView extends StatelessWidget {
  const CornerPostView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("Köşe yazıları yakında"),
      ),
    );
  }
}
